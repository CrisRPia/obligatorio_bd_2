name: Votes
services:
    votes_backend:
        image: votes_backend
        container_name: votes_backend
        build:
            context: ./backend
        ports:
            - "8080:8080"
        volumes:
            - ./backend/:/voting_backend/
        environment:
            - PING=pong!
        # depends_on:
        #   - db
